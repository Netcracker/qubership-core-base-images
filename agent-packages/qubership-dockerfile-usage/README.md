# qubership-dockerfile-usage

Agent-package with conventions for writing Dockerfiles for Qubership
platform microservices — base images, layer structure, security
hardening. The agent applies them automatically when creating or
editing a `Dockerfile` in a service repo.

events {}
http {
    server {
        listen 8080;
        location = /lua {
            default_type text/plain;
            content_by_lua_block {
                ngx.say("lua ok: " .. jit.version)
            }
        }
    }
}
events {}
http {
    server {
        listen 8080;
        location = /headers-more {
            default_type text/plain;
            more_set_headers "X-Test-Header: headers-more-ok";
            more_clear_headers "Server";
            content_by_lua_block {
                ngx.say("headers more module works")
            }
        }
    }
}

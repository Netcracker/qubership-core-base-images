events {}
http {
    server {
        listen 8080;

        # Include probes locations from base image
        include /etc/nginx/base-image-conf/probes-locations.conf;

        # Default location for testing
        location / {
            return 404;
        }
    }
}
